@model IEnumerable<C8.eServices.Mvc.Models.RCSApplicationStatus>
@using System.Text;
@using C8.eServices.Mvc.DataAccessLayer;
@using C8.eServices.Mvc.Keys

@{
    Layout = "~/Views/Shared/RCS_Layout.cshtml";
}
<style>
    .rowCrit {
        color: red !important;
    }
</style>
<script src="@Url.Content( "~/Scripts/moment.js" )"></script>
<div id="bankAccountPanel">
    <div class="panel panel-default">
        <div class="panel-heading">Department Approved Applications</div>
        <div style="width: 90%; margin: 0 auto;">
            <br />
            <table id="DepartmentsTable" class="table table-bordered table-hover table-striped panel panel-default display ">
                <thead>
                    <tr>
                        <th>
                            Reference Number
                        </th>
                        <th>
                            Status
                        </th>
                        <th>
                            Created Date
                        </th>
                        <th>
                            Action
                        </th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.ApplicationReferenceNumber)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Status.Name)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.CreatedDateTime)
                            </td>

                            <td>
                                @Html.ActionLink("Upload Assessment Figure Report", "UploadAssessmentFigure", new { id = item.Id }, new { @class = "btn btn-primary" })
                                @Html.ActionLink("Application Details", "Details", "Capture", new { refNo = item.Id, id = item.Id }, new { @class = "btn btn-primary" })
                            </td>
                        </tr>
                    }
            </tbody>
                
                </table>
        </div>
    </div>
</div>

@section Scripts{

    <script src="@Url.Content( "~/scripts/Datatables/jquery.dataTables.min.js" )"></script>
    <script src="@Url.Content( "~/scripts/Datatables/dataTables.bootstrap.js" )"></script>
}


<script>
    $(function () {
        $('#DepartmentsTable').DataTable();
    });
</script>